# decimal — "unvalidated field values" (moderate). Patched only in decimal
# 3.0.0, which requires an Ecto major upgrade (ecto ~> 3.12 caps decimal at
# ~> 2.0). rbac/ce is an internal gRPC service and does not parse
# attacker-controlled input into Decimal, so the impact is not reachable here.
# Accepted until the Ecto/decimal major upgrade is done separately.
GHSA-rhv4-8758-jx7v

# --- grpc 0.8.1 advisories, all first patched in grpc 1.0.0 ---
# This app runs an internal gRPC server (GRPC.Server.Supervisor); it is reachable only from
# inside the cluster by sibling Semaphore services, not from the internet. The fix is grpc
# 1.0.0 — a breaking major shared across the monorepo, tracked as a coordinated migration
# (see ee/audit/.mix-audit.txt), not a security-patch bump.
#
# RCE via :erlang.binary_to_term in GRPC.Codec.Erlpack.decode/2 (critical). Requires a server
# that registers the Erlpack codec and accepts application/grpc+erlpack; no app in this repo
# registers any custom codec (repo-wide grep for Erlpack/codecs: is empty), so the vulnerable
# decode path is unreachable.
GHSA-grp7-v8xh-rj7h
# Decompression bomb in GRPC.Compressor.Gzip.decompress/1 (high) — fires on frames arriving
# at a server with grpc-encoding: gzip; availability-only.
GHSA-6ccx-9c9f-327w
# Unbounded request-body accumulation in read_full_body/3 (high) — the server request read
# loop; availability-only.
GHSA-q8gf-9rvj-gmgj
# Path bindings overridable by query string/request body (high) — applies only to servers
# using HTTP transcoding path bindings (google.api.http); not used here.
GHSA-mwr4-5g34-j5cq

# --- gun 2.1.0 advisories, first patched in gun 2.4.0 ---
# gun is the gRPC client transport (via grpc/grpc_gun) to fixed internal service endpoints;
# no user-supplied hosts, paths or headers reach it. The gun version is pinned by the grpc
# dependency, so the bump rides the grpc 1.0.0 migration.
# unexpected status code / return value handling (high)
GHSA-2j82-37xg-f9wp
# uncontrolled resource consumption (high)
GHSA-r53j-fjj5-mv77
# gun_http2 origin validation error (moderate)
GHSA-36w4-95hv-5vwg
# HTTP request/response splitting shared with cowboy (moderate)
GHSA-w4f7-4cxr-rv3c

# --- mint 1.6.2 advisories (transitive via grpc) ---
# mint is a client-side HTTP library pulled in by grpc; there are no Mint call sites in lib/
# and outbound connections go to fixed internal endpoints, not attacker-chosen servers.
# unbounded CONTINUATION/HEADERS accumulation (high)
GHSA-2p26-p43x-fhp8
# unbounded streams map growth via PUSH_PROMISE (high)
GHSA-g586-ccqf-7x4r
# Content-Length accepts non-RFC "+" prefix (moderate)
GHSA-mjqx-c6f6-7rc2
# CRLF injection via unvalidated method/target (low)
GHSA-2pg6-44cx-c49v

# protobuf 0.13.0 — unbounded recursion decoding nested embedded messages
# (GHSA-rv48-qqj5-crxg, high), first patched in 0.16.1. protobuf only decodes messages from
# internal gRPC/AMQP peers, not attacker-supplied payloads, so the recursion DoS is not
# reachable here; the bump is held by the grpc-pinned protobuf line.
GHSA-rv48-qqj5-crxg

# --- family completion (2026-08-28) ---
# The scan syncs the advisory DB at run time, so per-advisory version ranges drift
# between runs (a PR run flagged ids the main run did not). The ids below complete each
# already-accepted package family so range widening cannot re-break the scan; each
# family's rationale is documented above or here.
# cowlib (family completion) — transitive via the grpc-pinned cowboy/gun stack; no SPDY,
# SSE or user-input cookies in this app.
GHSA-32p9-57cr-4x65
GHSA-84f2-rp86-235p
GHSA-hv23-4qp7-8c8r
GHSA-g2wm-735q-3f56
# rabbit_common — advisories target the RabbitMQ server and its Shovel/Federation plugins;
# this app is an AMQP client only (amqp/amqp_client) and the broker is deployed separately,
# so the affected server code never runs in this image.
GHSA-v9gv-xp36-jgj8
GHSA-w6cq-9cf4-gqpg
