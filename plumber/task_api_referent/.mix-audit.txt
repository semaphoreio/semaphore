# Advisories ignored here are transitive, unreachable in this app, and only fixable
# by an out-of-scope major bump of a core dependency. task_api_referent is an internal
# gRPC reference app used by plumber tests — no public HTTP surface. gun/cowboy/cowlib
# are pinned by grpc 0.3.1 (`gun ~> 1.2`, `cowboy ~> 2.5`); bumping any of them forces
# a cowboy/cowlib/ranch/gun stack bump on the old grpc adapter. The real fix —
# upgrading the plumber gRPC stack — is tracked in semaphoreio/semaphore#1134.

# cowboy 2.5.0 — unbounded buffer accumulation in multipart header parsing causes DoS
# (high). Patched in cowboy 2.15.0. The internal gRPC server does not parse multipart
# HTTP request bodies.
GHSA-jfc2-q6qh-g5x8

# cowlib 2.6.0 — three advisories, all patched in cowlib 2.16.1. None of the affected
# code paths are exercised by the internal gRPC server:
#   GHSA-32p9-57cr-4x65 — cow_http_te chunked Transfer-Encoding resource use
#   GHSA-84f2-rp86-235p — cow_spdy decompression bomb (SPDY is not used)
#   GHSA-hv23-4qp7-8c8r — CRLF injection / SSE event splitting (no SSE)
GHSA-32p9-57cr-4x65
GHSA-84f2-rp86-235p
GHSA-hv23-4qp7-8c8r

# gun 1.3.0 — two advisories, patched in gun 2.4.0. gun is the gRPC client transport
# for internal-service calls only; no user-controlled URLs, hosts, or request lines
# reach it.
#   GHSA-r53j-fjj5-mv77 — uncontrolled resource consumption, fixed in 2.4.0
#   GHSA-w4f7-4cxr-rv3c — HTTP request/response splitting (shared cowboy/gun advisory)
GHSA-r53j-fjj5-mv77
GHSA-w4f7-4cxr-rv3c

# --- grpc 0.3.1 advisories, all first patched in grpc 1.0.0 ---
# This app runs an internal gRPC server (GRPC.Server.Supervisor); it is reachable only from
# inside the cluster by sibling Semaphore services, not from the internet. The fix is grpc
# 1.0.0 — a breaking major shared across the monorepo, tracked as a coordinated migration
# (see ee/audit/.mix-audit.txt), not a security-patch bump.
#
# Unbounded request-body accumulation in read_full_body/3 (high) — the server request read
# loop; availability-only.
GHSA-q8gf-9rvj-gmgj

# --- family completion (2026-08-28) ---
# The scan syncs the advisory DB at run time, so per-advisory version ranges drift
# between runs (a PR run flagged ids the main run did not). The ids below complete each
# already-accepted package family so range widening cannot re-break the scan; each
# family's rationale is documented above or here.
# grpc (family completion) — same acceptance as above: internal-only listeners, no Erlpack
# codec registered repo-wide, no HTTP transcoding; real fix is the grpc 1.0.0 migration.
GHSA-grp7-v8xh-rj7h
GHSA-6ccx-9c9f-327w
GHSA-mwr4-5g34-j5cq
# gun (family completion) — client transport to fixed internal endpoints; bump rides the
# grpc 1.0.0 migration.
GHSA-2j82-37xg-f9wp
GHSA-36w4-95hv-5vwg
# cowlib (family completion) — transitive via the grpc-pinned cowboy/gun stack; no SPDY,
# SSE or user-input cookies in this app.
GHSA-g2wm-735q-3f56
