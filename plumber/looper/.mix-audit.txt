# Advisories ignored here are transitive, unreachable in this app, and only fixable
# by an out-of-scope major bump of a core dependency. looper is plumber's internal
# STM/polling library — no HTTP surface at all. Its cowboy 1.1.2 / plug 1.4.3 /
# hackney 1.10.1 / httpoison are pulled in transitively via the renderedtext github
# deps (util, watchman, tackle), not declared in mix.exs; bumping them means bumping
# those shared libraries fleet-wide.

# cowlib 1.0.2 — two advisories, patched in cowlib 2.16.1 (pulled via cowboy 1.1.2).
# looper runs no HTTP server; neither code path is reachable:
#   GHSA-32p9-57cr-4x65 — cow_http_te chunked Transfer-Encoding resource use
#   GHSA-84f2-rp86-235p — cow_spdy decompression bomb (SPDY is not used)
GHSA-32p9-57cr-4x65
GHSA-84f2-rp86-235p

# hackney 1.10.1 — six advisories. Two are patched in hackney 1.21.0/1.24.0, four only
# in the 4.0.1 major (blocked fleet-wide: httpoison pins hackney ~> 1.x). looper makes
# no outbound HTTP calls from lib/ — hackney is a transitive dependency of the shared
# watchman/util libraries only.
#   GHSA-9fm9-hp7p-53mf — connection pool release leak, fixed in 1.24.0
#   GHSA-vq52-99r9-h5pw — SSRF via redirect target, fixed in 1.21.0
#   GHSA-gp9c-pm5m-5cxr — ssl:connect/2 post-handshake upgrade has no timeout (4.0.1)
#   GHSA-j9wq-vxxc-94wf — CR/LF injection in query parameter (4.0.1)
#   GHSA-mp55-p8c9-rfw2 — CRLF/header injection via unvalidated domain/path options (4.0.1)
#   GHSA-pj7v-xfvx-wmjq — SSRF allowlist bypass via percent-encoded host (4.0.1)
GHSA-9fm9-hp7p-53mf
GHSA-vq52-99r9-h5pw
GHSA-gp9c-pm5m-5cxr
GHSA-j9wq-vxxc-94wf
GHSA-mp55-p8c9-rfw2
GHSA-pj7v-xfvx-wmjq

# plug 1.4.3 — unbounded buffer accumulation in multipart header parsing causes DoS
# (high), fixed in 1.15.4. looper serves no HTTP endpoints and parses no multipart
# bodies; plug is transitive via the shared renderedtext libraries.
GHSA-468c-vq7p-gh64

# postgrex 0.17.3 — channel-name SQL injection in Postgrex.Notifications.listen/3
# (high), fixed in 0.22.2, but ecto_sql 3.x pins postgrex < 0.22. looper never calls
# Postgrex.Notifications — it only pattern-matches %Postgrex.Result{} from Ecto raw
# queries with compile-time SQL.
GHSA-r73h-97w8-m54h

# decimal 2.1.1 — unbounded exponent DoS in Decimal.new, fixed in 3.0.0, but ecto/
# postgrex/jason all pin decimal < 3.0. decimal is not called anywhere in lib/.
GHSA-rhv4-8758-jx7v
