# cowlib — cookie request header injection in cow_cookie:cookie/1 (no patched
# version released upstream; cowlib 2.16.1 is the latest release and is still
# flagged). public-api/v1alpha does not generate Set-Cookie headers from
# user-controlled input; cowlib is only pulled in transitively via plug_cowboy/grpc.
GHSA-g2wm-735q-3f56

# decimal 2.4.1 — unbounded exponent DoS in Decimal.new (transitive via ecto/jason;
# service has no Ecto repo; Jason default decoder does not call Decimal.new)
GHSA-rhv4-8758-jx7v
