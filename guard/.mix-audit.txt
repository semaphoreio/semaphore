# hackney 1.22.0 — fails to release HTTP connections to pool
GHSA-9fm9-hp7p-53mf

# decimal 2.3.0 — DoS via unbounded exponent in Decimal.new
GHSA-rhv4-8758-jx7v

# postgrex 0.20.0 — channel-name SQL injection in Notifications.listen/3 (not used by guard)
GHSA-r73h-97w8-m54h

# --- hackney 1.22.0 advisories, all fixed in 4.0.1 ---
# Bump to 4.0.1 is blocked: httpoison ~> 1.x requires hackney ~> 1.17, so hackney cannot move to
# 4.x without a fleet-wide httpoison/grpc upgrade. Real fix tracked for the dep-bump branch.
#
# ssl:connect/2 post-handshake upgrade has no timeout (operational DoS; outbound bounded, same
# class as GHSA-9fm9 above)
GHSA-gp9c-pm5m-5cxr
# CR/LF injection in query parameter (no user-controlled data fed to hackney query params from lib/)
GHSA-j9wq-vxxc-94wf
# SSRF allowlist bypass in hackney_url:normalize/2 via percent-encoded host (no user-controlled URL
# fed to hackney from lib/)
GHSA-pj7v-xfvx-wmjq
# CRLF / header injection via unvalidated `domain` and `path` options (no user-controlled domain/path
# fed to hackney from lib/)
GHSA-mp55-p8c9-rfw2

# --- tesla 1.11.2 advisories, fixed in 1.18.3 ---
# Bump is blocked pending investigation: with tesla 1.20 the bitbucket oauth request
# phase regressed (empty redirect; caught by test/guard/id/api_test.exs) while the
# github/gitlab strategies kept working. tesla reaches guard only through
# oauth2 2.1.0 (tesla ~> 1.5) and the ueberauth_bitbucket fork's oauth2 usage; the
# exact mechanism is not yet isolated. Root-causing the regression and unblocking
# the bump is tracked as a follow-up; until then guard stays on ~> 1.11.0.
GHSA-28jh-g32x-v9v4
GHSA-9m9w-gxf7-rh8m
GHSA-h74c-q9j7-mpcm
GHSA-mc85-72gr-vm9f
GHSA-q7jx-v53g-848w

# --- grpc 0.5.0-beta.1 advisories, fixed in 1.0.0 ---
# Bump to 1.0.0 is a major jump off a beta, entangled with the fleet-wide httpoison/hackney/grpc
# upgrade noted above; it needs its own build + test pass and is tracked as an active follow-up.
# Interim suppression, per advisory:
#
# Erlpack codec binary_to_term RCE. NOT reachable here: guard registers no custom codec on any
# GRPC.Server (protobuf default only); the Erlpack codec is opt-in and never enabled.
GHSA-grp7-v8xh-rj7h
# gzip decompression bomb (DoS on the default path). Reachable only by an in-cluster caller of
# guard's gRPC; internet exposure of that port is gated by cluster ingress/firewall (see launchpad2).
GHSA-6ccx-9c9f-327w
# unbounded request-body accumulation (DoS on the default path). Same in-cluster-only reachability.
GHSA-q8gf-9rvj-gmgj

