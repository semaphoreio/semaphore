# cowlib 2.13.0 — cow_http_te DoS via excessive allocation (HTTP/1.1 reachable; gateway expected to normalize Transfer-Encoding before backend)
GHSA-32p9-57cr-4x65

# cowlib 2.13.0 — cow_cookie:cookie/1 outbound Cookie injection (service makes no outbound HTTP via cowboy/gun/cowlib path; gRPC clients do not build Cookie headers)
GHSA-g2wm-735q-3f56

# cowlib 2.13.0 — CRLF injection in header field values (plug 1.19.1 validates put_resp_header values; no user-tainted string reaches a response header)
GHSA-hv23-4qp7-8c8r

# hackney 1.20.1 — connection pool leak (no direct HTTPoison/hackney/Tesla call in lib/; pulled transitively only)
GHSA-9fm9-hp7p-53mf

# hackney 1.20.1 — SSRF via redirect target (no user-controlled URL fed to hackney; transitive dep only)
GHSA-vq52-99r9-h5pw

# decimal 2.1.1 — unbounded exponent DoS in Decimal.new (transitive via ecto/jason/logger_json/poison; service has no Ecto repo; default decoders do not call Decimal.new)
GHSA-rhv4-8758-jx7v

# plug_cowboy 2.7.2 — transitive cowboy vuln, fixed in 2.8.1
GHSA-q8x4-x7mp-5vg2

# cowboy 2.12.0 — multipart header parsing DoS, fixed in 2.15.0. Reachable: public_api/router.ex
# loads Plug.Parsers with {:multipart, length: 150_000_000}. Mitigated at cluster ingress L7 body
# limits; real fix tracked for the plug_cowboy dep-bump branch.
GHSA-jfc2-q6qh-g5x8

# cowlib 2.13.0 — SPDY decompression bomb in cow_spdy:inflate/2 (unreachable; cowboy 2.x does not serve SPDY)
GHSA-84f2-rp86-235p

# plug 1.16.1 — unbounded buffer accumulation in multipart header parsing, fixed in 1.16.3. Reachable:
# public_api/router.ex loads Plug.Parsers with {:multipart, length: 150_000_000}. Mitigated at cluster
# ingress L7 body limits; real fix tracked for the plug dep-bump branch.
GHSA-468c-vq7p-gh64

# --- hackney 1.x advisories, all first patched in hackney 4.0.1 ---
# The 4.x major is blocked fleet-wide: httpoison requires hackney ~> 1.17, so
# hackney cannot move to 4.x without a coordinated httpoison migration (same
# acceptance as plumber and guard). This service's hackney usage is limited to
# internal API calls.
GHSA-gp9c-pm5m-5cxr
GHSA-j9wq-vxxc-94wf
GHSA-mp55-p8c9-rfw2
GHSA-pj7v-xfvx-wmjq

# --- gun 2.1.0 advisories ---
# gun is the gRPC client transport for internal-service calls only; no user-controlled
# URLs, hosts, or request lines are fed to it. Bump tracked for the fleet-wide dep-bump branch.
#
# uncontrolled resource consumption, fixed in 2.4.0
GHSA-r53j-fjj5-mv77
# unexpected status/return value handling, fixed in 2.4.0
GHSA-2j82-37xg-f9wp
# gun_http2 origin validation error, fixed in 2.4.0
GHSA-36w4-95hv-5vwg
# HTTP request/response splitting (shared cowboy/gun advisory)
GHSA-w4f7-4cxr-rv3c

# protobuf 0.12.0 — unbounded recursion depth in embedded-message decoding, fixed in 0.16.1
# (decodes internal-service traffic only; bump tracked in the dedicated dependency PR)
GHSA-rv48-qqj5-crxg

# --- grpc 0.9.0 advisories, all first patched in grpc 1.0.0 ---
# grpc is used purely as a client (GRPC.Stub) against internal backend services: there is no
# `use GRPC.Server` and no GRPC.Server.Supervisor in lib/, so the server-side code paths
# below are not started here. The fix is grpc 1.0.0 — a breaking major shared across the
# monorepo, tracked as a coordinated migration (see ee/audit/.mix-audit.txt).
#
# RCE via :erlang.binary_to_term in GRPC.Codec.Erlpack.decode/2 (critical). Requires a server
# that registers the Erlpack codec and accepts application/grpc+erlpack; no app in this repo
# registers any custom codec (repo-wide grep for Erlpack/codecs: is empty), so the vulnerable
# decode path is unreachable.
GHSA-grp7-v8xh-rj7h
# Decompression bomb in GRPC.Compressor.Gzip.decompress/1 (high) — fires on frames arriving
# at a server with grpc-encoding: gzip; availability-only.
GHSA-6ccx-9c9f-327w
# Unbounded request-body accumulation in read_full_body/3 (high) — the server request read
# loop; availability-only.
GHSA-q8gf-9rvj-gmgj
# Path bindings overridable by query string/request body (high) — applies only to servers
# using HTTP transcoding path bindings (google.api.http); not used here.
GHSA-mwr4-5g34-j5cq
