# postgrex 0.17.1 — channel-name SQL injection in Postgrex.Notifications.listen/3 (service does not use Postgrex.Notifications; only standard Ecto queries)
GHSA-r73h-97w8-m54h

# cowlib 2.11.0 — cow_http_te DoS via excessive allocation (gRPC over HTTP/2 only; cow_http_te HTTP/1.1 path unreachable)
GHSA-32p9-57cr-4x65

# cowlib 2.11.0 — cookie header injection in cow_cookie:cookie/1 (gRPC clients do not build Cookie headers)
GHSA-g2wm-735q-3f56

# cowlib 2.11.0 — CRLF / SSE injection in header field values (gRPC server, no Plug response-header writes from user input; no SSE)
GHSA-hv23-4qp7-8c8r

# cowboy 2.9.0 — multipart header parsing DoS, fixed in 2.15.0 (unreachable; gRPC-only service over HTTP/2, no Plug.Parsers, no multipart endpoints)
GHSA-jfc2-q6qh-g5x8

# cowlib 2.11.0 — SPDY decompression bomb in cow_spdy:inflate/2 (unreachable; cowboy 2.x does not serve SPDY)
GHSA-84f2-rp86-235p

# --- grpc 0.5.0 (via :grpc) — all first patched in 1.0.0 ---
# The 1.0 major is a fleet-wide migration (codec/stub API changes plus pb
# regeneration), deferred to a dep-bump branch.
#
# RCE via attacker-controlled payloads: the sink is
# GRPC.Codec.Erlpack.decode/2 (:erlang.binary_to_term). The Cowboy handler
# resolves a codec from the request content-type subtype but only among the
# codecs registered on that server, and `codecs = opts[:codecs] ||
# [GRPC.Codec.Proto]` — the scheduler registers none, so an
# application/grpc+erlpack request is rejected with "No codec registered"
# and binary_to_term is never reached. Not reachable without a code change.
GHSA-grp7-v8xh-rj7h
# Unbounded request body accumulation in read_full_body/3 — availability only,
# and only from an in-cluster caller: the gRPC listener on 50050 is ClusterIP,
# no ambassador mapping targets it, so ingress body limits are not in the path.
GHSA-q8gf-9rvj-gmgj
# Unbounded gzip decompression (decompression bomb) on request bodies —
# availability only, same internal-only reach, same 1.0.0 fix.
GHSA-6ccx-9c9f-327w

# --- protobuf 0.11.0 ---
# Unbounded recursion depth decoding embedded messages, patched in 0.16.1.
# The scheduler's schemas contain no cyclic message (39 messages, no cycle)
# and no Any/Struct/Value field, so decode depth is bounded by the schema
# itself. Both decode paths take internal input: gRPC from sibling services,
# and the OrgBlocked/OrgUnblocked AMQP consumers from the internal exchange.
# Bump needs pb regeneration against the 0.16 codegen.
GHSA-rv48-qqj5-crxg

# --- hackney 1.18.1 — all first patched in 4.0.1 ---
# 4.x is blocked fleet-wide by httpoison ~> 1.x. The scheduler makes no
# outbound HTTP from lib/ (no HTTPoison or :hackney call sites); hackney
# arrives transitively, and its targets are service-configured.
GHSA-9fm9-hp7p-53mf
GHSA-gp9c-pm5m-5cxr
GHSA-j9wq-vxxc-94wf
GHSA-mp55-p8c9-rfw2
GHSA-pj7v-xfvx-wmjq
GHSA-vq52-99r9-h5pw

# --- decimal 2.1.1 ---
# Unbounded exponent DoS in Decimal.new, patched in 3.0.0 (transitive via
# ecto; the bump cascades into an ecto major). No Decimal.new call sites in
# lib/ — the scheduler parses no untrusted decimal strings.
GHSA-rhv4-8758-jx7v
