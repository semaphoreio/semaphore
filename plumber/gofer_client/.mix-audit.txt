# Advisories ignored here are transitive, unreachable in this app, and only fixable
# by an out-of-scope major bump of a core dependency. gofer_client is plumber's
# internal gRPC client library for gofer (promotions) — no public HTTP surface.
# gun/cowboy/cowlib are pinned by grpc 0.3.1 (`gun ~> 1.2`, `cowboy ~> 2.5`); bumping
# any of them forces a cowboy/cowlib/ranch/gun stack bump on the old grpc adapter.
# The real fix — upgrading the plumber gRPC stack — is tracked in
# semaphoreio/semaphore#1134.

# cowboy 2.5.0 — unbounded buffer accumulation in multipart header parsing causes DoS
# (high). Patched in cowboy 2.15.0. cowboy is only pulled in as grpc's server-side
# dependency; this library runs no HTTP server and parses no multipart bodies.
GHSA-jfc2-q6qh-g5x8

# cowlib 2.6.0 — three advisories, all patched in cowlib 2.16.1. None of the affected
# code paths are exercised by a gRPC client:
#   GHSA-32p9-57cr-4x65 — cow_http_te chunked Transfer-Encoding resource use
#   GHSA-84f2-rp86-235p — cow_spdy decompression bomb (SPDY is not used)
#   GHSA-hv23-4qp7-8c8r — CRLF injection / SSE event splitting (no SSE)
GHSA-32p9-57cr-4x65
GHSA-84f2-rp86-235p
GHSA-hv23-4qp7-8c8r

# gun 1.2.0 — two advisories, patched in gun 2.4.0. gun is the gRPC client transport
# for internal gofer calls only; no user-controlled URLs, hosts, or request lines
# reach it.
#   GHSA-r53j-fjj5-mv77 — uncontrolled resource consumption, fixed in 2.4.0
#   GHSA-w4f7-4cxr-rv3c — HTTP request/response splitting (shared cowboy/gun advisory)
GHSA-r53j-fjj5-mv77
GHSA-w4f7-4cxr-rv3c

# --- grpc 0.3.1 advisories, all first patched in grpc 1.0.0 ---
# grpc is used purely as a client (GRPC.Stub) against internal backend services: there is no
# `use GRPC.Server` and no GRPC.Server.Supervisor in lib/, so the server-side code paths
# below are not started here. The fix is grpc 1.0.0 — a breaking major shared across the
# monorepo, tracked as a coordinated migration (see ee/audit/.mix-audit.txt).
#
# Unbounded request-body accumulation in read_full_body/3 (high) — the server request read
# loop; availability-only.
GHSA-q8gf-9rvj-gmgj
