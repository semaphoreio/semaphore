# Generated by usvc-1.15.0
# Feel free to adjust, it will not be overridden

defmodule TaskApiReferent.Mixfile do
  use Mix.Project

  def project do
    [
      app: :task_api_referent,
      version: "0.2.0",
      elixir: "~> 1.11",
      build_embedded: Mix.env == :prod,
      start_permanent: Mix.env == :prod,
      deps: deps(),
      aliases: aliases()
    ]
  end

  # Configuration for the OTP application
  #
  # Type "mix help compile.app" for more information
  def application do
    [
      extra_applications: [:logger, :runtime_tools],
      env: [mix_env: Mix.env()],
      mod: {TaskApiReferent.Application, []}
    ]
  end

  # Dependencies can be Hex packages:
  #
  #   {:my_dep, "~> 0.3.0"}
  #
  # Or git/path repositories:
  #
  #   {:my_dep, git: "https://github.com/elixir-lang/my_dep.git", tag: "0.1.0"}
  #
  # Type "mix help deps" for more examples and options
  defp deps do
    [
      {:sys2app, github: "renderedtext/sys2app"},
      {:credo, "~> 1.4", only: [:dev, :test], runtime: false},
      {:proto, path: "../proto"},
      {:util, github: "renderedtext/elixir-util"},
      {:log_tee, git: "https://github.com/renderedtext/log-tee.git"},
      {:uuid, "~> 1.1"},
      {:jason, "~> 1.1"},
      {:grpc, "~> 0.3"},
      {:protobuf, "~> 0.5.4"},
      {:junit_formatter, "~> 3.1", only: [:test]}
    ]
  end

  defp aliases() do
    [
      "deps.local": ["local.hex --force", "local.rebar --force"],
      "deps.setup": ["deps.local", "deps.get"],
      setup: ["deps.setup"]
    ]
  end
end
