# notifications runs internal gRPC servers plus outbound webhook/Slack
# delivery. cowboy/cowlib arrive transitively via grpc; hackney via httpoison.
# Real dependency bumps are tracked for the fleet-wide dep-bump branch.

# --- grpc 0.5.0-beta.1 — all first patched in 1.0.0 ---
# The 1.0 major is a fleet-wide migration (codec/stub API changes plus pb
# regeneration), deferred to a dep-bump branch.
#
# RCE via attacker-controlled payloads: the sink is
# GRPC.Codec.Erlpack.decode/2 (:erlang.binary_to_term). The Cowboy handler
# resolves a codec from the request content-type subtype but only among the
# codecs registered on that server, and `codecs = opts[:codecs] ||
# [GRPC.Codec.Proto]` — notifications registers none, so an
# application/grpc+erlpack request is rejected with "No codec registered"
# and binary_to_term is never reached. Not reachable without a code change.
GHSA-grp7-v8xh-rj7h
# Unbounded request body accumulation in read_full_body/3 — availability only,
# and only from an in-cluster caller: the gRPC listeners serve internal
# services and are not exposed through ingress.
GHSA-q8gf-9rvj-gmgj
# Unbounded gzip decompression (decompression bomb) on request bodies —
# availability only, same internal-only reach, same 1.0.0 fix.
GHSA-6ccx-9c9f-327w

# cowboy 2.7.0 — multipart header parsing DoS, fixed in 2.15.0 (unreachable;
# gRPC-only inbound surface over HTTP/2, no Plug.Parsers, no multipart
# endpoints)
GHSA-jfc2-q6qh-g5x8

# cowlib 2.11.0 — cow_http_te DoS via excessive allocation (gRPC over HTTP/2
# only; cow_http_te HTTP/1.1 path unreachable)
GHSA-32p9-57cr-4x65

# cowlib 2.11.0 — SPDY decompression bomb in cow_spdy:inflate/2 (unreachable;
# cowboy 2.x does not serve SPDY)
GHSA-84f2-rp86-235p

# cowlib 2.11.0 — cookie header injection in cow_cookie:cookie/1 (gRPC
# servers/clients do not build Cookie headers)
GHSA-g2wm-735q-3f56

# cowlib 2.11.0 — CRLF / SSE injection in header field values (gRPC server, no
# Plug response-header writes from user input; no SSE)
GHSA-hv23-4qp7-8c8r

# --- hackney 1.18.1 — all first patched in 4.0.1 ---
# 4.x is blocked fleet-wide by httpoison ~> 1.x. Unlike most services, these
# ARE reachable here by design: Workers.Webhook and Workers.Slack pass
# customer-configured endpoint URLs to HTTPoison/hackney (with
# follow_redirect: false and bounded connect/recv timeouts). This is a risk
# acceptance, not an unreachability claim — the real mitigation (webhook-URL
# host allowlisting plus the hackney 4.x bump) is tracked in
# renderedtext/tasks#10462.
#
# ssl:connect/2 post-handshake upgrade has no timeout (operational DoS;
# bounded by worker retry limits)
GHSA-gp9c-pm5m-5cxr
# CR/LF injection in query parameter
GHSA-j9wq-vxxc-94wf
# fails to properly release HTTP connections (resource leak; bounded workload)
GHSA-9fm9-hp7p-53mf
# CRLF / header injection via unvalidated `domain` and `path` options
GHSA-mp55-p8c9-rfw2
# Server-side Request Forgery (customer-supplied webhook targets; allowlist
# fix tracked as above)
GHSA-vq52-99r9-h5pw
# SSRF allowlist bypass in hackney_url:normalize/2 via percent-encoded host
GHSA-pj7v-xfvx-wmjq

# postgrex 0.16.2 — channel-name SQL injection in Postgrex.Notifications.listen/3
# (service does not call Postgrex.Notifications; only standard Ecto queries.
# Bumping past 0.16.x cascades into an ecto bump that is out of scope here)
GHSA-r73h-97w8-m54h

# decimal 2.0.0 — unbounded exponent DoS in Decimal.new, patched in 3.0.0
# (transitive via ecto/postgrex; no Decimal call sites in lib/ — the service
# parses no untrusted decimal strings)
GHSA-rhv4-8758-jx7v
