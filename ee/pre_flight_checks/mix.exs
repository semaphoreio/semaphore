# Generated by usvc-1.17.0
# Feel free to adjust, it will not be overridden

defmodule PreFlightChecks.MixProject do
  use Mix.Project

  def project do
    [
      app: :pre_flight_checks,
      version: "0.1.0",
      elixir: "~> 1.13",
      start_permanent: Mix.env() == :prod,
      deps: deps(),
      aliases: aliases(),
      releases: releases()
    ]
  end

  # Run "mix help compile.app" to learn about applications.
  def application do
    [
      mod: {PreFlightChecks.Application, []},
      extra_applications: [:logger, :runtime_tools, :watchman, :tackle]
    ]
  end

  # Dependencies can be Hex packages:
  #
  #   {:my_dep, "~> 0.3.0"}
  #
  # Or git/path repositories:
  #
  #   {:my_dep, git: "https://github.com/elixir-lang/my_dep.git", tag: "0.1.0"}
  #
  # Type "mix help deps" for more examples and options
  defp deps do
    [
      {:ecto_sql, "~> 3.7"},
      {:ecto, "~> 3.7"},
      {:jason, "~> 1.3"},
      {:postgrex, "~> 0.15"},
      {:uuid, "~> 1.1"},
      {:vmstats, "~> 2.4"},
      {:grpc, "0.5.0-beta.1", override: true},
      {:amqp, "~> 1.2", override: true},
      {:tackle, github: "renderedtext/ex-tackle"},
      {:util, github: "renderedtext/elixir-util"},
      {:log_tee, github: "renderedtext/log-tee"},
      {:watchman, github: "renderedtext/ex-watchman"},
      {:credo, "~> 1.5", only: [:dev, :test], runtime: false},
      {:grpc_mock, github: "renderedtext/grpc-mock", only: [:dev, :test]},
      {:junit_formatter, "~> 3.1", only: [:test]}
    ]
  end

  defp aliases() do
    [
      "deps.local": ["local.hex --force", "local.rebar --force"],
      "deps.setup": ["deps.local", "deps.get"],
      "ecto.reset": ["ecto.drop", "ecto.setup"],
      "ecto.setup": ["ecto.create", "ecto.migrate"],
      setup: ["deps.setup", "ecto.setup"]
    ]
  end

  defp releases do
    [
      pre_flight_checks: [
        include_executables_for: [:unix]
      ]
    ]
  end
end
