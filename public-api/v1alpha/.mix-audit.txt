# cowlib 2.6.0 — cow_http_te DoS via excessive allocation (HTTP/1.1 reachable; gateway expected to normalize Transfer-Encoding before backend; bump blocked by grpc 0.3.1 / cowboy 2.5)
GHSA-32p9-57cr-4x65

# cowlib 2.6.0 — CRLF injection in header field values (reachable via /logs/:job_id Location redirect; auth-gated self-target; track code-level job_id sanitization separately)
GHSA-hv23-4qp7-8c8r

# hackney 1.18.1 — connection pool leak (operational only; outbound bounded to Sentry DSN + tzdata IANA URL)
GHSA-9fm9-hp7p-53mf

# hackney 1.18.1 — SSRF via redirect target (no user-controlled URL fed to hackney; HTTPoison not called from lib/)
GHSA-vq52-99r9-h5pw

# decimal 2.1.1 — unbounded exponent DoS in Decimal.new (transitive via ecto/jason; service has no Ecto repo; Jason default decoder does not call Decimal.new)
GHSA-rhv4-8758-jx7v
