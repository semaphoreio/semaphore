# cowlib — cookie request header injection in cow_cookie:cookie/1 (no patched
# version released upstream; zebra does not generate Set-Cookie headers from
# user input — it only serves a small internal Plug.Cowboy plane).
GHSA-g2wm-735q-3f56

# postgrex — channel-name SQL injection in Postgrex.Notifications.listen/3.
# zebra does not call Postgrex.Notifications anywhere (grep `Postgrex.Notifications`
# in lib/ returns no hits). Bumping postgrex past 0.16.x would cascade into an
# ecto/Elixir version bump that is out of scope for a security patch PR.
GHSA-r73h-97w8-m54h

# hackney 1.25.0 — four URL/TLS advisories all first patched in hackney 4.0.1: ssl:connect/2
# post-handshake upgrade has no timeout (GHSA-gp9c-pm5m-5cxr), CR/LF injection in query param
# (GHSA-j9wq-vxxc-94wf), CRLF/header injection via domain/path options (GHSA-mp55-p8c9-rfw2), and
# SSRF allowlist bypass via percent-encoded host (GHSA-pj7v-xfvx-wmjq). hackney 4.0.1 is a major
# upgrade blocked by httpoison ~> 1.17 / sentry (1.6.5 or ~> 1.8) — both cap hackney < 2.0; no
# httpoison/sentry release uses hackney 4.x. Not reachable: outbound goes to fixed internal service
# endpoints + the Sentry DSN — no user-controlled host/query/path is fed to hackney.
GHSA-gp9c-pm5m-5cxr
GHSA-j9wq-vxxc-94wf
GHSA-mp55-p8c9-rfw2
GHSA-pj7v-xfvx-wmjq

# --- grpc 0.5.0-beta.1 advisories, all first patched in grpc 1.0.0 ---
# zebra does run gRPC servers (internal_job_api, internal_task_api, public_job_api and the
# health check), so unlike front these are not dead code paths. Both are accepted for now: the
# bump to 1.0.0 is a breaking major shared across the monorepo — front/.mix-audit.txt lists zebra
# among the nine apps in the coordinated migration — and does not belong on an incident branch.
#
# UNREACHABLE — RCE / atom-table exhaustion via :erlang.binary_to_term in
# GRPC.Codec.Erlpack.decode/2 (critical). Requires a server that registers the Erlpack codec and
# accepts application/grpc+erlpack; zebra registers no custom codec (grep for Erlpack, GRPC.Codec
# and `codecs:` across lib/ and config/ returns nothing), so the decode path is never entered.
GHSA-grp7-v8xh-rj7h

# Availability-only — unbounded gzip decompression (decompression bomb). zebra's gRPC endpoints
# are cluster-internal: they are called by sibling services, and the public jobs API is reached
# only through public-api-gateway, which terminates the end-user request and issues its own gRPC
# call rather than forwarding the caller's payload. No end-user-supplied compressed body reaches
# this decoder.
GHSA-6ccx-9c9f-327w

# Availability-only — unbounded request-body accumulation in read_full_body/3, the server request
# read loop. Same exposure as the gzip advisory above: bounded to callers that can reach zebra's
# gRPC endpoints, which is sibling services and the gateway, not end users directly.
GHSA-q8gf-9rvj-gmgj
