

export enum Status {
  Empty = `empty`,
  Loading = `loading`,
  Loaded = `loaded`,
  Error = `error`,
}

export enum BranchType {
  All = `all`,
  Default = `default`,
}

export enum BuildStatus {
  All = `all`,
  Success = `success`,
}
