# cowlib — cookie request header injection in cow_cookie:cookie/1 (no patched
# version released upstream). ee/rbac does not generate Set-Cookie headers from
# user-controlled input; cowlib is only pulled in via plug_cowboy/grpc.
GHSA-g2wm-735q-3f56

# protobuf — unbounded recursion depth in embedded-message decoding (patched in
# 0.16.1). The bump is tracked in a dedicated dependency PR: it must also drop
# the vendored Google.Protobuf.Timestamp module (protobuf ships the well-known
# types since 0.14, so keeping the generated copy fails --warnings-as-errors)
# and adjust pb.gen accordingly. ee/rbac only decodes internal-service traffic.
GHSA-rv48-qqj5-crxg

# postgrex — channel-name SQL injection in Postgrex.Notifications.listen/3.
# ee/rbac does not call Postgrex.Notifications anywhere (grep `Postgrex.Notifications`
# in lib/ returns no hits). Bumping postgrex past 0.19.x would cascade into an
# ecto/Elixir version bump that is out of scope for a security patch PR.
GHSA-r73h-97w8-m54h

# decimal — unbounded exponent in `Decimal.new` (patched in 3.0.0). postgrex 0.19.3
# pins decimal to `~> 2.0`, so bumping to 3.0 would cascade into a postgrex/ecto
# bump that is out of scope for a security patch PR. ee/rbac does not parse
# untrusted decimal strings.
GHSA-rhv4-8758-jx7v

# --- hackney 1.25.0 advisories, all fixed in 4.0.1 ---
# Bump to 4.0.1 is blocked: httpoison 2.x requires hackney ~> 1.17, so hackney cannot move to
# 4.x without a fleet-wide httpoison/grpc upgrade. Real fix tracked for the dep-bump branch.
# ee/rbac's only outbound HTTP is via Tesla to configured provider base URLs (GitHub, Bitbucket,
# OIDC) — hosts and paths are static config or server-issued identifiers, never free-form user input.
#
# ssl:connect/2 post-handshake upgrade has no timeout (operational DoS; outbound calls only, to
# trusted provider endpoints)
GHSA-gp9c-pm5m-5cxr
# CR/LF injection in query parameter (no user-controlled data fed to hackney query params from lib/)
GHSA-j9wq-vxxc-94wf
# SSRF allowlist bypass in hackney_url:normalize/2 via percent-encoded host (base URLs are static
# config, no user-controlled URL fed to hackney from lib/)
GHSA-pj7v-xfvx-wmjq
# CRLF / header injection via unvalidated `domain` and `path` options (no user-controlled domain/path
# fed to hackney from lib/)
GHSA-mp55-p8c9-rfw2

# gun 2.4.1 — HTTP request/response splitting (shared cowboy/gun advisory; the <2.16.0 range is
# cowboy's numbering and over-matches gun). gun is only pulled in as the grpc client transport and
# ee/rbac's gRPC traffic is internal service-to-service with no user-controlled request lines or
# headers. Real fix tracked for the fleet-wide dep-bump branch.
GHSA-w4f7-4cxr-rv3c

# --- grpc 0.8.1 — all first patched in 1.0.0 ---
# The 1.0 major is a fleet-wide migration (codec/stub API changes plus pb
# regeneration), deferred to a dep-bump branch.
#
# RCE via attacker-controlled payloads: the sink is
# GRPC.Codec.Erlpack.decode/2 (:erlang.binary_to_term). The handler resolves a
# codec from the request content-type subtype, but only among the codecs
# registered on the server, and the 0.8.x default list is
# [GRPC.Codec.Proto, GRPC.Codec.WebText, GRPC.Codec.JSON] — erlpack is not in
# it and ee/rbac passes no `codecs:`, so the request is rejected before
# decoding. Not reachable without a code change.
GHSA-grp7-v8xh-rj7h
# Path bindings overridable by query string / request body: path bindings only
# exist under HTTP/JSON transcoding, which is opt-in (`http_transcode`).
# ee/rbac's gRPC servers declare no http_transcode option, so there are no
# bindings to override.
GHSA-mwr4-5g34-j5cq
# Unbounded request body accumulation in read_full_body/3 — availability only,
# and only from an in-cluster caller: the gRPC listener serves internal
# services and is not exposed through ingress.
GHSA-q8gf-9rvj-gmgj
# Unbounded gzip decompression (decompression bomb) — same internal-only
# reach, availability only.
GHSA-6ccx-9c9f-327w
