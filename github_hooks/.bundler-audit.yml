---
ignore:
  - CVE-2024-26143
  - CVE-2024-34341
  - CVE-2026-38969 # webrick 1.9.2 trailer reparse; no patched release yet
  # selenium-webdriver < 4.14.0 NULL pointer dereference (CVE-2023-5590).
  # Test-only dependency (Gemfile group :test); never ships to production.
  # The ~> 2.53 -> 4.x move is a breaking Capybara-stack upgrade, tracked
  # separately.
  - GHSA-4phc-fq33-39gx
