# gofer dependency-audit suppressions.
#
# gofer is a gRPC service. cowlib/cowboy are pulled in only via the grpc/cowboy/gun stack, which is
# pinned by the gRPC adapter; postgrex/decimal come from the Ecto stack. Every advisory below is
# cleared only by an out-of-scope stack bump tracked for the dep-bump branch.

# --- cowlib 2.11.0 (fixed only in cowlib 2.18.0, needs cowboy 2.17 / gun 2.4 / ranch 2.2) ---
# cow_http_te uncontrolled resource consumption / excessive allocation (high).
GHSA-32p9-57cr-4x65
# cow_spdy:inflate/2 decompression bomb (high). SPDY is not enabled.
GHSA-84f2-rp86-235p
# Cookie request-header injection in cow_cookie:cookie/1 (low). gofer does not build outbound Cookie
# request headers from user-controlled input.
GHSA-g2wm-735q-3f56
# CRLF injection / SSE event splitting via unvalidated field values (moderate).
GHSA-hv23-4qp7-8c8r

# --- cowboy 2.9.0 ---
# Unbounded buffer accumulation in multipart header parsing DoS (high). Fixed in cowboy 2.13.0;
# gofer serves gRPC, not multipart HTTP uploads.
GHSA-jfc2-q6qh-g5x8

# --- postgrex 0.16.3 ---
# Channel-name SQL injection in Postgrex.Notifications.listen/3 (high). listen/3 is not used by
# gofer (no Postgrex.Notifications usage in lib/). Fix requires a larger postgrex bump.
GHSA-r73h-97w8-m54h

# --- decimal 2.0.0 ---
# Unbounded exponent in Decimal.new enables unauthenticated DoS (moderate). Fix is decimal v3, a
# breaking major. No user-controlled input reaches Decimal.new.
GHSA-rhv4-8758-jx7v
