# cowlib — cookie request header injection in cow_cookie:cookie/1 (no patched
# version released upstream; cowlib 2.16.1 is the latest release and is still
# flagged). repository_hub is a gRPC service and does not generate Set-Cookie
# headers from user-controlled input; cowlib is only pulled in via grpc/cowboy/gun.
GHSA-g2wm-735q-3f56

# postgrex — channel-name SQL injection in Postgrex.Notifications.listen/3.
# repository_hub does not call Postgrex.Notifications anywhere (grep returns no hits);
# it uses only standard Ecto queries. Bumping postgrex to 0.22.2 cascades into an
# ecto bump out of scope for a security patch.
GHSA-r73h-97w8-m54h

# decimal — unbounded exponent DoS in Decimal.new, patched in 3.0.0 (transitive via
# ecto; bumping to 3.0 cascades into an ecto bump out of scope. repository_hub does
# not parse untrusted decimal strings — no Decimal.new calls in lib/).
GHSA-rhv4-8758-jx7v

# --- hackney 1.x advisories, all first patched in hackney 4.0.1 ---
# The 4.x major is blocked fleet-wide: httpoison requires hackney ~> 1.17, so
# hackney cannot move to 4.x without a coordinated httpoison migration (same
# acceptance as plumber and guard). repository_hub's outbound HTTP targets the
# configured git providers and internal services only.
GHSA-gp9c-pm5m-5cxr
GHSA-j9wq-vxxc-94wf
GHSA-mp55-p8c9-rfw2
GHSA-pj7v-xfvx-wmjq

# --- gun 2.0.1 advisories, all first patched in gun 2.4.0 ---
# gun is the gRPC client transport (grpc dep). Every outbound gun connection
# targets a fixed internal endpoint from env config (encryptor sidecar,
# user/organization/repository_integrator services) — plaintext HTTP/2 inside
# the cluster, no user-controlled URLs, no proxies, no cross-origin connection
# reuse, and the far end is a trusted internal service. Bumping the gun version
# pin is out of scope for this dependency-debt allowlist pass.
# GHSA-2j82-37xg-f9wp (high): unexpected status/return value — protocol
#   confusion needs an attacker-influenced or proxied endpoint; none exist.
GHSA-2j82-37xg-f9wp
# GHSA-36w4-95hv-5vwg (moderate): gun_http2 origin validation — needs
#   connection reuse across origins; channels here are per-endpoint.
GHSA-36w4-95hv-5vwg
# GHSA-r53j-fjj5-mv77 (high): uncontrolled resource consumption — requires a
#   malicious/compromised internal server flooding the client; worst case is
#   one pod's memory, supervisor restarts.
GHSA-r53j-fjj5-mv77
# GHSA-w4f7-4cxr-rv3c (moderate, also cowboy < 2.16): HTTP request/response
#   splitting — CRLF header injection is an HTTP/1.1 concern; both the cowboy
#   listener and gun clients here speak binary-framed HTTP/2 on the internal
#   network with no user-controlled header values.
GHSA-w4f7-4cxr-rv3c

# protobuf — unbounded recursion depth decoding embedded messages, patched in
# 0.16.1. repository_hub only decodes protobufs from internal gRPC callers on
# the cluster network; a hostile deeply-nested payload requires a compromised
# internal service and yields a single-pod DoS (supervisor restarts). Bumping
# needs pb regeneration against the 0.16 codegen — out of scope for this pass.
GHSA-rv48-qqj5-crxg

