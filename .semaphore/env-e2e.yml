version: v1.0
name: E2E Tests - ${{parameters.CLOUD_TEST_ENV_PREFIX}} - ${{parameters.CLOUD_TEST_ENVIRONMENT_TYPE}}
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2004
blocks:
  - name: E2E Tests
    task:
      jobs:
        - name: E2E tests - API
          commands:
            - echo 1
        - name: E2E tests - UI
          commands:
            - echo 1
