# Generated by usvc-1.13.3
# Feel free to adjust, it will not be overridden

defmodule PipelinesAPI.Mixfile do
  use Mix.Project

  def project do
    [
      app: :pipelines_api,
      version: "0.1.0",
      elixir: "~> 1.4",
      elixirc_paths: elixirc_paths(Mix.env()),
      build_embedded: Mix.env() == :prod,
      start_permanent: Mix.env() == :prod,
      deps: deps()
    ]
  end

  # Configuration for the OTP application
  #
  # Type "mix help compile.app" for more information
  def application do
    # Specify extra applications you'll use from Erlang/Elixir
    [extra_applications: [:logger], mod: {PipelinesAPI.Application, []}]
  end

  # Specifies which paths to compile per environment.
  defp elixirc_paths(:test), do: ["lib", "test/support"]
  defp elixirc_paths(:dev), do: ["lib", "test/support"]
  defp elixirc_paths(_), do: ["lib"]

  # Dependencies can be Hex packages:
  #
  #   {:my_dep, "~> 0.3.0"}
  #
  # Or git/path repositories:
  #
  #   {:my_dep, git: "https://github.com/elixir-lang/my_dep.git", tag: "0.1.0"}
  #
  # Type "mix help deps" for more examples and options
  defp deps do
    [
      {:grpc, "~> 0.3"},
      {:plug_cowboy, "~> 2.0"},
      {:poison, "~> 3.1"},
      {:httpoison, "~> 1.3.0"},
      {:credo, "~> 1.5", only: [:dev, :test], runtime: false},
      {:log_tee, github: "renderedtext/log-tee"},
      {:uuid, "~> 1.1"},
      {:watchman, github: "renderedtext/ex-watchman"},
      {:lager, "~> 3.6"},
      {:scrivener_headers, "~> 3.1"},
      {:scrivener, "~> 2.3"},
      {:util, github: "renderedtext/elixir-util"},
      {:wormhole, "~> 2.3", override: true},
      {:yaml_elixir, "~> 2.1"},
      {:grpc_mock, github: "renderedtext/grpc-mock", only: [:dev, :test]},
      {:mix_test_watch, "~> 1.0", only: [:dev, :test], runtime: false},
      {:timex, "~> 3.1", only: [:test], runtime: false},
      {:ecto, "~> 3.8.4", only: [:dev, :test], runtime: false},
      {:ex_crypto, github: "ntrepid8/ex_crypto", ref: "0997a1aaebe701523c0a9b71d4acec4a1819354e"},
      {:faker, "~> 0.17", only: [:dev, :test]},
      {:fun_registry, github: "renderedtext/fun-registry", only: [:dev, :test]},
      {:dialyxir, "~> 1.1", only: [:dev], runtime: false},
      {:junit_formatter, "~> 3.1", only: [:test]},
      {:cachex, "~> 3.2"},
      {:feature_provider, git: "git@github.com:renderedtext/feature_provider", tag: "v0.2.1"}
    ]
  end
end
