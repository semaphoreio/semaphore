# cowlib — cookie request header injection in cow_cookie:cookie/1 (no patched
# version released upstream; cowlib 2.16.1 is the latest release and is still
# flagged). repository_hub is a gRPC service and does not generate Set-Cookie
# headers from user-controlled input; cowlib is only pulled in via grpc/cowboy/gun.
GHSA-g2wm-735q-3f56

# postgrex — channel-name SQL injection in Postgrex.Notifications.listen/3.
# repository_hub does not call Postgrex.Notifications anywhere (grep returns no hits);
# it uses only standard Ecto queries. Bumping postgrex to 0.22.2 cascades into an
# ecto bump out of scope for a security patch.
GHSA-r73h-97w8-m54h

# decimal — unbounded exponent DoS in Decimal.new, patched in 3.0.0 (transitive via
# ecto; bumping to 3.0 cascades into an ecto bump out of scope. repository_hub does
# not parse untrusted decimal strings — no Decimal.new calls in lib/).
GHSA-rhv4-8758-jx7v
