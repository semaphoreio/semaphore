# postgrex 0.17.1 — channel-name SQL injection in Postgrex.Notifications.listen/3 (service does not use Postgrex.Notifications; only standard Ecto queries)
GHSA-r73h-97w8-m54h

# cowlib 2.11.0 — cow_http_te DoS via excessive allocation (gRPC over HTTP/2 only; cow_http_te HTTP/1.1 path unreachable)
GHSA-32p9-57cr-4x65

# cowlib 2.11.0 — cookie header injection in cow_cookie:cookie/1 (gRPC clients do not build Cookie headers)
GHSA-g2wm-735q-3f56

# cowlib 2.11.0 — CRLF / SSE injection in header field values (gRPC server, no Plug response-header writes from user input; no SSE)
GHSA-hv23-4qp7-8c8r

# cowboy 2.9.0 — multipart header parsing DoS, fixed in 2.15.0 (unreachable; gRPC-only service over HTTP/2, no Plug.Parsers, no multipart endpoints)
GHSA-jfc2-q6qh-g5x8

# cowlib 2.11.0 — SPDY decompression bomb in cow_spdy:inflate/2 (unreachable; cowboy 2.x does not serve SPDY)
GHSA-84f2-rp86-235p

# --- grpc 0.5.0, all three first patched in grpc 1.0.0 ---
# The remedy is grpc 1.0.0, a major upgrade from the 0.5 line: it changes the grpc-elixir API and
# cascades to gun (~> 2.0), cowboy/cowlib and the generated .pb.ex stubs. Separately tracked
# migration, not a security-patch bump.

# GHSA-grp7-v8xh-rj7h (critical) — RCE / atom-table exhaustion via GRPC.Codec.Erlpack, which calls
# :erlang.binary_to_term/1 without :safe. Not reachable: the codec is only used when listed in the
# `codecs:` option; grpc 0.5.0 defaults to [GRPC.Codec.Proto], and neither Scheduler.Grpc.Server nor
# Scheduler.Grpc.HealthCheck.Server passes `codecs:`. Another content-subtype is answered with
# :unimplemented before any decoding happens.
GHSA-grp7-v8xh-rj7h

# GHSA-6ccx-9c9f-327w (high) — decompression bomb in GRPC.Compressor.Gzip. Not reachable: compressors
# default to [] and neither server passes `compressors:`, so find_compressor/2 answers :unimplemented
# for any grpc-encoding, and a frame with the compressed flag set is rejected when no compressor is bound.
GHSA-6ccx-9c9f-327w

# GHSA-q8gf-9rvj-gmgj (high) — unbounded body accumulation in the cowboy handler's read_full_body/3,
# which concatenates every chunk with no size cap; a client that omits grpc-timeout also gets an
# :infinity read deadline. This one IS reachable: it is the default unary path, needs no configuration,
# and the server runs without an auth interceptor, so any peer that can open a connection to the gRPC
# port can grow the process heap until it is killed. Impact is denial of service for this service only
# — no data exposure and no code execution — and the process is restarted automatically. Accepted here
# because the remedy is the same grpc 1.0.0 upgrade; limiting who can reach the port is handled outside
# this repository.
GHSA-q8gf-9rvj-gmgj

# protobuf 0.11.0 — GHSA-rv48-qqj5-crxg (high), unbounded recursion depth in embedded-message decoding,
# fixed in 0.16.1. Not reachable with the schemas this service decodes: the attack needs a self-referential
# or cyclic message type so that the attacker controls the nesting depth through the input bytes. No
# InternalApi module compiled into this service forms such a cycle (maximum static nesting depth is 5),
# and none use google.protobuf.Struct / Value / ListValue / Any. Bumping to 0.16.1 also changes the
# generated-code API and requires regenerating every .pb.ex module — a separately tracked migration.
GHSA-rv48-qqj5-crxg
