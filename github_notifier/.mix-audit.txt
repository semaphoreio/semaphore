# --- hackney 1.20.1 advisories, all fixed in 4.0.1 ---
# Bump to 4.x is blocked here: httpoison ~> 1.x/2.x requires hackney ~> 1.17 and
# sentry 8.x caps hackney at 1.x, so hackney cannot move to 4.x without upgrading
# httpoison to 3.x and sentry to 13.x together. Real fix tracked for a separate
# dep-bump PR. github_notifier's outbound HTTP (tentacat/httpoison/hackney)
# targets the GitHub API and Sentry with service-configured URLs, not
# user-supplied hosts/queries.
#
# ssl:connect/2 post-handshake upgrade has no timeout (operational DoS; outbound bounded to known hosts)
GHSA-gp9c-pm5m-5cxr
# CR/LF injection in query parameter (query params are not user-controlled)
GHSA-j9wq-vxxc-94wf
# fails to properly release HTTP connections (resource leak; bounded workload)
GHSA-9fm9-hp7p-53mf
# CRLF / header injection via unvalidated `domain` and `path` options (not user-controlled)
GHSA-mp55-p8c9-rfw2
# Server-side Request Forgery (target hosts are not user-supplied)
GHSA-vq52-99r9-h5pw
# SSRF allowlist bypass in hackney_url:normalize/2 via percent-encoded host (hosts not user-supplied)
GHSA-pj7v-xfvx-wmjq

# --- protobuf 0.13.0 ---
# Unbounded recursion depth in embedded-message decoding, fixed in 0.16.1.
# Bump requires regenerating all internal_api stubs; messages decoded here come
# from internal services over the private network, not untrusted input. Real fix
# tracked for the same separate dep-bump PR.
GHSA-rv48-qqj5-crxg

# --- gun 2.4.1: mix_audit false positive ---
# GHSA-w4f7-4cxr-rv3c (CRLF injection in cowlib structured-field headers)
# affects cowboy < 2.16.0 and gun < 2.4.0 per the advisory's own
# per-package ranges. This lockfile has cowboy 2.17.0, gun 2.4.1 and
# cowlib 2.18.0 - all at or above the patched versions. mix_audit
# collapses the multi-package ranges into one list and matches gun
# 2.4.1 against cowboy's "< 2.16.0" bound, so any gun version gets
# flagged. Additionally, gun here only carries gRPC (HTTP/2, binary
# framing) to internal services with fixed paths and internally
# generated metadata - no user-controlled header values.
GHSA-w4f7-4cxr-rv3c

# --- grpc 0.9.0 advisories, all first patched in grpc 1.0.0 ---
# This app runs an internal gRPC server (GRPC.Server.Supervisor); it is reachable only from
# inside the cluster by sibling Semaphore services, not from the internet. The fix is grpc
# 1.0.0 — a breaking major shared across the monorepo, tracked as a coordinated migration
# (see ee/audit/.mix-audit.txt), not a security-patch bump.
#
# RCE via :erlang.binary_to_term in GRPC.Codec.Erlpack.decode/2 (critical). Requires a server
# that registers the Erlpack codec and accepts application/grpc+erlpack; no app in this repo
# registers any custom codec (repo-wide grep for Erlpack/codecs: is empty), so the vulnerable
# decode path is unreachable.
GHSA-grp7-v8xh-rj7h
# Decompression bomb in GRPC.Compressor.Gzip.decompress/1 (high) — fires on frames arriving
# at a server with grpc-encoding: gzip; availability-only.
GHSA-6ccx-9c9f-327w
# Unbounded request-body accumulation in read_full_body/3 (high) — the server request read
# loop; availability-only.
GHSA-q8gf-9rvj-gmgj
# Path bindings overridable by query string/request body (high) — applies only to servers
# using HTTP transcoding path bindings (google.api.http); not used here.
GHSA-mwr4-5g34-j5cq

# --- family completion (2026-08-28) ---
# The scan syncs the advisory DB at run time, so per-advisory version ranges drift
# between runs (a PR run flagged ids the main run did not). The ids below complete each
# already-accepted package family so range widening cannot re-break the scan; each
# family's rationale is documented above or here.
# gun (family completion) — client transport to fixed internal endpoints; bump rides the
# grpc 1.0.0 migration.
GHSA-2j82-37xg-f9wp
GHSA-r53j-fjj5-mv77
GHSA-36w4-95hv-5vwg
# cowlib (family completion) — transitive via the grpc-pinned cowboy/gun stack; no SPDY,
# SSE or user-input cookies in this app.
GHSA-32p9-57cr-4x65
GHSA-84f2-rp86-235p
GHSA-hv23-4qp7-8c8r
GHSA-g2wm-735q-3f56
# mint (family completion) — transitive via grpc, no Mint call sites in lib/, outbound to
# fixed internal endpoints only.
GHSA-2p26-p43x-fhp8
GHSA-g586-ccqf-7x4r
GHSA-mjqx-c6f6-7rc2
GHSA-2pg6-44cx-c49v
# rabbit_common — advisories target the RabbitMQ server and its Shovel/Federation plugins;
# this app is an AMQP client only (amqp/amqp_client) and the broker is deployed separately,
# so the affected server code never runs in this image.
GHSA-v9gv-xp36-jgj8
GHSA-w6cq-9cf4-gqpg
