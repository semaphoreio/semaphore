defmodule RepositoryHub.Server.Bitbucket.CheckDeployKeyActionTest do
  @moduledoc false
  use RepositoryHub.ServerActionCase, async: true
  @tag :skip
  test "I'm tested" do
    assert false
  end
end
